import ApplePayService from '../../../src/checkout-com/services/apple-pay.service';

describe('ApplePayService tests', () => {
    test('apple pay service exists', () => {
        expect(ApplePayService).toBeTruthy();
    });
});
